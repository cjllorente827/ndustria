Other Notes
============

* Any function without a return statement or with an empty return statment will be run again everytime. 

.. ## `.pack()`
.. ndustria includes a routine `.pack()` which when called on a pipeline object can allow you to package the tarball the stashed returns for your pipeline so that you can more easily inspect those files or so that you can port them over onto another machine.

